# Copy this file to .env and fill values for local development
PORT=8000
# If you want to use a local MongoDB for development set the following URL.
# Example (default DB name `zuba`):
MONGODB_LOCAL_URI=mongodb://localhost:27017/zuba

# If you prefer Atlas, set MONGODB_URI instead (keep secure, don't commit credentials)
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/<dbname>?retryWrites=true&w=majority

# Email + other settings (copy from your existing .env)
# EMAIL=
# EMAIL_PASS=
# JSON_WEB_TOKEN_SECRET_KEY=
# ...
